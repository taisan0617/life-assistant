# Production environment — served via CloudFront.
# URLs are relative; CloudFront routes /chat → Lambda and /api/* → API Gateway.
VITE_CHAT_STREAM_URL=/chat
VITE_HISTORY_API_URL=/api

# Fill in after `terraform output` (same values as .env.local)
VITE_COGNITO_USER_POOL_ID=us-east-1_J9v6XT2dB
VITE_COGNITO_APP_CLIENT_ID=1ohkrgf6jmorv7kfjbnnolmcvb
VITE_COGNITO_REGION=us-east-1
